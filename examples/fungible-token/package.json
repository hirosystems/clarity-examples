{
  "name": "fungible-token",
  "version": "2.0.0",
  "description": "fungible token smart contract example",
  "scripts": {
    "test": "vitest run -- --manifest-path ./Clarinet.toml",
    "test:reports": "vitest run -- --coverage --costs",
    "test:watch": "chokidar \"unit-tests/**/*.ts\" \"contracts/**/*.clar\" -c \"npm t\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hirosystems/clarinet-sdk": "^1.3.0",
    "@stacks/transactions": "^6.11.3",
    "chokidar-cli": "^3.0.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.4",
    "vitest": "^1.3.1",
    "vitest-environment-clarinet": "^1.1.0"
  }
}
